networks:
  grpc-api:
    name: grpc-api

volumes:
  data:
    driver: local

services:
  database:
    image: mysql
    env_file: .env
    networks:
      - grpc-api
    ports:
      - "3306:3306"
    volumes:
      - data:/var/lib/mysql
